apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: cache-aside-flask-postgres-redis
  labels:
    app.kubernetes.io/part-of: cache-aside-flask-postgres-redis
    app.kubernetes.io/component: postgres
type: Opaque
stringData:
  POSTGRES_USER: crud
  POSTGRES_PASSWORD: crud
  POSTGRES_DB: crud
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: cache-aside-flask-postgres-redis
  labels:
    app.kubernetes.io/part-of: cache-aside-flask-postgres-redis
    app.kubernetes.io/component: postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: cache-aside-flask-postgres-redis
  labels:
    app.kubernetes.io/part-of: cache-aside-flask-postgres-redis
    app.kubernetes.io/component: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
        app.kubernetes.io/part-of: cache-aside-flask-postgres-redis
        app.kubernetes.io/component: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          envFrom:
            - secretRef:
                name: postgres-credentials
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
              subPath: pgdata
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: cache-aside-flask-postgres-redis
  labels:
    app.kubernetes.io/part-of: cache-aside-flask-postgres-redis
    app.kubernetes.io/component: postgres
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
