resources:
  - namespace.yaml
  - postgres.yaml
  - backend.yaml
  - ingress.yaml
builds:
  - name: backend
    contextPath: app
    dockerfilePath: app/Dockerfile
    image: crud-flask-postgres/backend
