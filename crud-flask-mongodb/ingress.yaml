apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend
  namespace: crud-flask-mongodb
  labels:
    app.kubernetes.io/part-of: crud-flask-mongodb
    app.kubernetes.io/component: backend
spec:
  rules:
    - host: crud-flask-mongodb.greencap.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 80
